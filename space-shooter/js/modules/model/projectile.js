export default class Projectile{
    constructor(x, y, color, direction, speed) {
        this.x = x;
        this.y = y;
        this.speed = speed;
        this.color = color;
        this.direction = direction;
    }
}